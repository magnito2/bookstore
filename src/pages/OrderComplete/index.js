import React from "react";
import { useParams } from "react-router-dom";

import UpdateOrder from "../../components/CompleteOrder";


const OrderComplete = ({}) => {
    return (
        <div>
            <UpdateOrder />
        </div>
    )
}

export default OrderComplete;